# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/road_quality_monitoring

# Redis Configuration (ephemeral session/socket state)
# Local development: redis://localhost:6379
# Docker Compose: redis://redis:6379
# Azure Cache for Redis: rediss://your-cache.redis.cache.windows.net:6380
# AWS ElastiCache: redis://your-cluster.cache.amazonaws.com:6379
REDIS_URL=redis://localhost:6379

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRY=7d

# Map Matching API (OSRM)
MAP_MATCHING_API_URL=http://router.project-osrm.org/match/v1/driving

# Mapbox Configuration (for route scoring)
MAPBOX_ACCESS_TOKEN=your_mapbox_access_token_here

# Route Scoring Configuration
ROUTE_QUALITY_WEIGHT=0.6
ROUTE_DISTANCE_WEIGHT=0.4
ROUTE_CACHE_TTL=3600

# Geohash Configuration
GEOHASH_PRECISION=6

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Socket.IO Configuration
SOCKET_PING_TIMEOUT=60000
SOCKET_PING_INTERVAL=25000

# Data Aggregation
TIME_DECAY_HOURS=24
MIN_OBSERVATIONS_FOR_AGGREGATION=3
